// extern crate ndarray;
// extern crate ndarray_stats;

// use ndarray_stats::errors::MultiInputError;
// use ndarray::Array1;

// use crate::ndarray_stats::DeviationExt;

// pub fn mean_absolute_error(preds: &Array1<f64>, actual: &Array1<f64>) -> Result<f64, MultiInputError> {
//     preds.mean_abs_err(actual)
// }


pub mod neural_network;